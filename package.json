{
  "name": "e-fuel-monorepo",
  "version": "1.0.0",
  "description": "Root package to run both frontend and backend concurrently",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run web --prefix frontend\"",
    "db:local": "npm run db:local --prefix backend",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "@types/react": "~19.1.10",
    "concurrently": "^8.2.2",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "mongodb": "^7.2.0"
  }
}
